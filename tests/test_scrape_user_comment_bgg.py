import unittest

# from scripts.scrape_user_comment_bgg import get_top_X_game_ids


class TestScriptScrapeUserCommentBGG(unittest.TestCase):
    
    def test_get_top_X_game_ids(self):
        # num_games = 5
        # game_ids = get_top_X_game_ids(num_games)
        # game_ids = []
        # self.assertEqual(len(game_ids), num_games)
        # self.assertTrue(all(game_id.isdigit() for game_id in game_ids))
        # self.assertTrue(all(len(game_id) > 0 for game_id in game_ids))
        pass